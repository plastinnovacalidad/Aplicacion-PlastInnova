One-line: non-blocking confirmation; the coloured element is the icon only, never the card background.

```jsx
<Toast tone="success" title="Cotización enviada" message="Te respondemos por WhatsApp en minutos." onClose={dismiss} />
```
