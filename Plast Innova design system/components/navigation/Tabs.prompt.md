One-line: switches between sibling views — the active underline is the only place brand red appears in navigation.

```jsx
<Tabs items={['Descripción','Especificaciones','Envío']} />
```

`variant="pill"` for compact in-card switching.
