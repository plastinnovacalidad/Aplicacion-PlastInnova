One-line: uppercase pill for product flags ("NUEVO", "-20%") and status.

```jsx
<Badge tone="accent" solid>-20%</Badge>
```

Tones: brand, accent, neutral, success, warning, info. Use `solid` only on imagery where the tinted version would not read.
