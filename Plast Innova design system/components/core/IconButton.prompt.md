One-line: circular icon-only button for toolbars, card overlays and dialog dismissals.

```jsx
<IconButton icon="heart" label="Guardar" variant="outline" />
```

Always pass `label`. `ghost` for in-toolbar use, `outline` on product cards, `solid`/`accent` when it is a real action.
