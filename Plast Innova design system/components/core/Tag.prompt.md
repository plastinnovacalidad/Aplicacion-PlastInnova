One-line: 32px filter chip used in catalogue sidebars and applied-filter rows.

```jsx
<Tag selected onRemove={() => {}}>Luz fría 6000K</Tag>
```

Differs from Badge: Tag is interactive and sentence-case; Badge is static and uppercase.
