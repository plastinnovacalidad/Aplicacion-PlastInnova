One-line: the standard white content container — hairline border plus a soft blue-tinted shadow, 16px corners.

```jsx
<Card interactive elevation="sm">…</Card>
```

Set `padding="0"` when the card holds a full-bleed image. Never stack more than one elevation step above the surrounding surface.
