One-line: the standard text field — 10px corners, hairline border, blue focus ring.

```jsx
<Input label="Correo" iconLeft="mail" placeholder="tu@correo.com" hint="Te escribimos solo por tu pedido." />
```

Pass `error` to swap the hint for a red message and recolour the border.
